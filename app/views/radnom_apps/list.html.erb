<div class="form">
  <table>
    <thead>
      <tr>
        <th>Names</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @radnom_apps.each do |radnom_app| %>
        <tr>
          <td><%= radnom_app.name %></td>
<!--           <td><%= link_to 'Show', radnom_app %></td> -->
          <td><%= link_to 'Edit', edit_radnom_app_path(radnom_app) %></td>
<!--           <td><%= link_to 'Destroy', radnom_app, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<div class="form_buttons">
<!--   <%= link_to 'Add name', new_radnom_app_path %>
  <br> -->
  <%= link_to 'Back to the wheel!', wheel_path %>
</div>
